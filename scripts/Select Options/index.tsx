/**
 * This script is intended to be used as an Assistant Tool.
 * Add it in Assistant tool settings and trigger from assistant conversation.
 */
import { Script } from "scripting"

Script.exit({
  success: true,
  message: "Select Options is an Assistant Tool script.",
})
